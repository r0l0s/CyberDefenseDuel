package Application;

public class rest {

	public rest() {
		/*
		//Player Figure creation
		//Creating a rectangle object         
		Rectangle rectangle = new Rectangle();
		rectangle.setX(50.0f); 
		rectangle.setY(100.0f); 
		rectangle.setWidth(75.0f); 
		rectangle.setHeight(75.0f);  
		Group rect = new Group(rectangle);
		
		//Coordenadas del bichillo
		double x = rectangle.getX();
		double y = rectangle.getY();
		int counter = 1;
		TranslateTransition tt = new TranslateTransition(Duration.millis(2000), rect);     
		
		
		rectangle.setOnKeyPressed((new EventHandler<KeyEvent>() { 
			   public void handle(KeyEvent event) { 
				   if (event.getCode() == KeyCode.A) {
					   System.out.println("Move izq");
					   double nuevaX = rectangle.getTranslateX() - 4;
				        
				        rectangle.setTranslateX(nuevaX);
					   
				   }
				   if (event.getCode() == KeyCode.D) {
					   System.out.println("Move der"); 
					   double nuevaX = rectangle.getTranslateX() + 4;
				        
				        rectangle.setTranslateX(nuevaX);
				   }
				   if (event.getCode() == KeyCode.W) {
					   System.out.println("Move arr"); 
				   }
				   if (event.getCode() == KeyCode.S) {
					   System.out.println("Move aba"); 
				   }
			   } 
			}));
		
		
		rectangle.setFocusTraversable(true);
		*/
	}

}
